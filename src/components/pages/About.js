import React from 'react';

function About() {
  return (
    <React.Fragment>
      <h1>About</h1>
      <p>
        This is a simple to do list app created as part of self learning react
        framework. we can add, delete or mark todos as completed or not, since
        this is just a web app in react and dont have a back end or data base
        the todos will be refershed after we refresh the web page.
      </p>
    </React.Fragment>
  );
}

export default About;
